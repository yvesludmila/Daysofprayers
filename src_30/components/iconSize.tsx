import { Dimensions } from "react-native";

const { width, height } = Dimensions.get('window')

export const iconSmall = width * 0.05;
export const iconMedium = width * 0.07;
export const iconLarge = width * 0.1;