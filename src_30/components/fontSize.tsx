import { Dimensions } from "react-native";

const { width, height } = Dimensions.get('window')

export const fontSizeSmall = width * 0.038;
export const fontSizeMedium = width * 0.04;
export const fontSizeLarge = width * 0.05;