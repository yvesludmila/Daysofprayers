import { StyleSheet, Dimensions } from "react-native";
import { gray, greenWhite } from "../components/color";

const indexStyle = StyleSheet.create({
    container: {
        flex: 1,
        width: "100%",
        height: "100%",
        // justifyContent: "center",
        backgroundColor: gray
    }
})

export default indexStyle;